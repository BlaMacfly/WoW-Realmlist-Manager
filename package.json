{
  "name": "realmlist-switcher",
  "version": "1.0.0",
  "description": "Application Electron pour la gestion des realmlists",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . RealmlistSwitcher --platform=win32 --arch=x64 --out=dist --overwrite --icon=icon.ico"
  },
  "dependencies": {
    "howler": "^2.2.4"
  },
  "devDependencies": {
    "electron": "^27.0.0",
    "electron-packager": "^17.1.2"
  }
}

